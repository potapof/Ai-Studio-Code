# Studio Handoff — 2026-08-10 → …

Состояние на момент: студия проверена/починена (ИТ1-ИТ6), архивариус оживлён,
readiness-связка готова. maxscrm — активный проект (t8 CI-стабильность).

---

## 2026-08-12/13 — maxscrm t8 ЗАКРЫТ: полный зелёный CI на self-hosted

Ранн 31708663002 (b069c34): 13/13 джоб SUCCESS (k6/DAST — schedule-only по дизайну).
Впервые зелёный ВЕСЬ конвейер: Jest 170, Vitest, E2E Playwright 32/32, Mobile E2E,
Build&Scan (cosign), Gitleaks, Trivy (кэш DB), SAST, DepAudit, Docs.

ПРОДУКТОВЫЙ БАГ (найден E2E-зондами): PrismaService.client внутри withTenant (RLS)
возвращает tx-хендл без $transaction → создание заказов/товаров/категорий → 500.
Фикс: PrismaService.$transaction (root client) + 8 ERP-сервисов (b069c34).

E2E-инфра: vite proxy localhost→127.0.0.1 (IPv6 ::1 без фолбэка); playwright workers=2,
expect 10s, убран extraHTTPHeaders Content-Type (Fastify «Body cannot be empty»);
спеки под новый сид (/s/demo), идемпотентность (unique email/title), storeId+slug,
store-guard; trivy vuln-DB кэш + --skip-db-update; GH services → локальные docker run.

ОПС-уроки: runner OOM (MemoryHigh 8G/Max 12G в systemd-юнитах, jest --maxWorkers=2),
pkill -f убивает собственную оболочку (паттерн в своей командной строке).

БЗ: loop_runs 19 (этот цикл), сессия hermes-ci-stability. Staging = 335dd17
(деплои отменялись при CI-фиксах; app-код с 335dd17 НЕ менялся — только тесты/CI/бэкенд-фикс
$transaction, требует деплоя на staging).

---

## 2026-08-14 — «делай по порядку»: 4 пункта выполнены

1. ДЕПЛОЙ фикса: staging обновлён (ранн 31787028597 SUCCESS) — $transaction-фикс + идемпотентный сид.
   health healthy, /s/demo 200, демо-товары нового сида видны. Причина двух падений деплоя:
   (а) сид НЕ идемпотентен для store (create вместо wipe) — исправлен wipe-по-slug (seed.ts);
   (б) prisma generate без ретраев (fetch-engine ETIMEDOUT) — ретраи в Dockerfile (f6c26be).
2. РОТАЦИЯ СЕКРЕТОВ: 6 новых GitHub-секретов (STAGING_DB_PASSWORD/JWT/REDIS/MINIO/WEBHOOK/GRAFANA)
   вместо СЛАБЫХ ЗАКОММИЧЕННЫХ дефолтов (staging-jwt-secret...!) в deploy-staging.yml.
   Пароль БД: volume-reset fallback деплоя обрабатывает ротацию. SSH-ключ/MAX_BOT_TOKEN — не тронуты
   (требуют доступа к хосту/платформе MAX). Сгенерировано через libsodium (публичный ключ репо).
3. ПРЕЗЕНТАЦИЯ: docs/07-business/31_INVESTOR-PITCH.html — 15 слайдов (1920×1080, Weimob-модель,
   план 14 + «Команда» с плейсхолдерами). Проверено: консоль чистая, JS OK, навигация работает.
4. PROMETHEUS /metrics: был НЕПОЛНЫЙ (31 дефолтная, бизнес-метрики не появлялись).
   Причины: (а) PrometheusMetricsService в 2 модулях → 2 registry (app vs crm);
   (б) trackHttpRequest кэшировал по ключу с лейблами → дубль-регистрация при 2-м запросе (баг!).
   Фикс: @Global() MetricsModule + APP_INTERCEPTOR (http_requests/duration) + кэш по имени.
   Живая проверка: 34 семейства, leads_total{orgId,stage}, http_requests_total{method,path,status}.
   Юнит-тесты 170 passed. Закоммичено 58bbbc5, линт-фикс 43ca451 (HEAD).

---

## ⚠ ЧЕКПОИНТ ПЕРЕЗАГРУЗКИ (2026-08-14, ~13:30 MSK)

Пользователь перезагружает компьютер. Что было в полёте на момент остановки:

- HEAD develop = 43ca451 (метрики + линт-фикс). CI push (31792621180) и Deploy Staging
  (31792621230) — QUEUED (раннеры упадут при ребуте → ранны могут быть помечены failed/cancelled).
- Локально работали: node-бэкенд dist/src/main.js (:3000, метрики 34), vite (:5173),
  docker: e2e-pg (5432, засижен), e2e-redis (56379), maxscrm-postgres (dev-БД), nocodb,
  gateway :8010, herdr daemon, actions-runner{,2} (systemd user + linger).

ПОСЛЕ ПЕРЕЗАГРУЗКИ (по порядку):
1. СТАРТОВЫЙ РИТУАЛ: ~/studio/scripts/studio-readiness.sh → ПОКАЗАТЬ полный вывод.
2. Проверить раннеры: systemctl --user status actions-runner actions-runner-2
   (должны подняться сами через linger; при необходимости: systemctl --user restart …).
3. Docker-контейнеры: docker start maxscrm-postgres (и прочие dev-контейнеры, если не
   в restart=always); e2e-pg/e2e-redis не критичны (для E2E-локальных прогонов).
4. CI/деплой: проверить ранны 31792621180 / 31792621230 (вероятно failed из-за ребута).
   Если failed → повторно: dispatch Deploy Staging (метрики 43ca451 должны уйти на staging!)
   и при необходимости CI push. СМОТРЕТЬ ТОКЕН: git remote URL (x-access-token), НЕ ~/studio/.env
   (у него нет доступа к maxscrmru).
5. Верификация staging: https://app.maxscrm.ru/api/v1/health + /s/demo 200 +
   /api/v1/health/metrics → ожидать 34+ семейства (метрики после деплоя).
6. Локальный dev-стек (опционально): node dist/src/main.js (entry = dist/src/main.js,
   НЕ dist/main!) + vite --host 127.0.0.1 --port 5173 — только если нужен локальный прогон.

СЕКРЕТЫ (не терять): ротированные значения в /tmp/staging-rotation.env (0600) — на tmp,
после ребута ФАЙЛА НЕ БУДЕТ; значения уже в GitHub-секретах (это источник истины).
