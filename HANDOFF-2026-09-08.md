# HANDOFF — 2026-09-08

Сессия: Hermes Агент, ~/studio. Сдача состояния для новой сессии.

## Что сделано

### 1. Mobile E2E ЗАКРЫТ (maxscrm develop, коммит `c303e24`)
- Причина: логин «Войти в MAXSCRM» → `GET /api/v1/auth/max` → 302 на внешний MAX OAuth
  (deep-link max.ru) → не завершается в headless → `waitForURL('**/dashboard')` таймаут.
- Фикс: новый `apps/frontend/e2e/login.ts` — `loginDev(page)`: `POST /api/v1/auth/dev
  {maxUserId:'1', orgSlug:'default'}` (создаёт org+user на лету) → кладёт JWT в SPA через
  `useAuthStore.devLogin` → SPA-переход на /dashboard. Без внешнего OAuth.
  Подключён в `mobile-nav.spec.ts` + `mobile-audit.spec.ts`.
- Верификация: локально 8 passed / 1 skipped; tsc 0; lint 0.
  CI-ран `34195470408`: E2E Tests SUCCESS + Mobile E2E SUCCESS (после rerun-failed-jobs;
  первое падение — сетевая флака на pnpm install / binaries.prisma.sh ECONNRESET).

### 2. Питфолл: коллизия портов
Локальный dev-backend :3000 / frontend :5173 затеняют CI-контейнеры на той же VM
(--network host) → ложные 429/400/404/500 в critical-paths.
**Перед CI-прогонами dev-стек выключать** + `docker rm -f backend e2e-pg e2e-redis`.

### 3. Студия поднята
backend :3000 + frontend :5173 + gateway :8010 + OpenDesign :7456, чеклист 21/21 GREEN.

### 4. Библиотека архифай-схем (коммиты запушены)
- **maxscrm** (develop, head=`35e98a7`) — `docs/archify/`:
  app-architecture, ci-cd, login, demo-data, order, maxpay-webhook, prod-deploy, integrations + README.
  Коммиты: `4e2cbaf`, `566035d` (E2E-фиксы), `46759b3`, `6b380ff`, `35e98a7`.
- **Ai-Studio-Code** (main, head=`52623d0`) — `~/studio/docs/archify/`:
  design-studio, design-cycle, design-delegation, design-data, design-open-design-cycle,
  loop-engineering, studio-agents, studio-brain, studio-docker-stack + README.
  Коммиты `0ac5b35`, `52623d0`.

Все схемы: `archify validate --quality showcase` OK (0 диагностик) + deliver OK.
Часть высоких (workflow/lifecycle/sequence с ветками) — containment-скролл при visual-check
(общий лимит архифая, то же у эталонных примеров); validate+deliver зелёные.

## Состояние
- maxscrm develop: CI зелёный (E2E + Mobile E2E success). Рабочее дерево чистое,
  кроме untracked `docs/archify/*.html/.png/visual-check` (не коммитим).
- Ai-Studio-Code main: запушен. Untracked: `docs/archify/*.html/.png`.
  **Не тронуты unrelated**: HANDOFF-*.md, `docs/16-design-studio.md` (modified), `Ai/`.
- Студия: dev-стек ВКЛЮЧЁН.

## Как пушить (важно)
- **Ai-Studio-Code**: SSH-remote ЛОМАЕТСЯ (Host key verification failed) → пушить через
  `https://x-access-token:${GITHUB_TOKEN}@github.com/potapof/Ai-Studio-Code.git`
  (GITHUB_TOKEN в `~/studio/.env`).
- **maxscrm**: обычный `git push origin develop` (remote с токеном).

## Что делать в новой сессии
1. **Старт-ритуал**: `bash ~/studio/scripts/studio-readiness.sh` → показать чеклист + вердикт.
2. Схемы готовы; по желанию открыть (`xdg-open ~/studio/docs/archify/<file>.html`)
   или собрать ещё (мультитенантность, безопасность, handoff-driven development).
3. Если предстоят CI-прогоны на maxscrm — выключить dev-стек + почистить CI-контейнеры.

## Опционально / не сделано
- storefront-admin логин (storefront.spec.ts) остался на внешнем OAuth — проходит, но та же
  флаки-класс; можно перевести на `loginDev`.
- Авто-деплой на мерж в main — НЕ трогал (только по явному слову; меняет прод-конфиг).
- Расхождение демо-каталога content.ts vs seed.js — опц.

## Лессон по архифай-law-ту (для скилла)
viewBox ширина ≤ ~1085 (иначе composition/desktop-readability), ≥ H×1.16 (иначе containment-overflow
для высоких типов); workflow v2 принудительная высота ≥786/912; sequence сообщения в пределах
читаемой шкалы; избегать `**/` в комментариях (закрывает блок-комментарий).
