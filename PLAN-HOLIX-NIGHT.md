# PLAN — Ночной прогон: рабочий агентский слой «Студии»

Дата старта: 2026-07-11 (ночь). Режим: полная автономия, выполнять итерации подряд.
Принципы: реальная проверка каждого шага; не фабриковать; секреты не светить; откат
через backups/; рекурсивные кроны не создавать.

ЦЕЛЬ НОЧИ: чтобы Hermes (хост) мог делегировать задачу Holix-воркеру, и тот РЕАЛЬНО
её исполнил (агентский прогон через gateway), + привести состав агентов к замыслу
(python-dev/react-dev), + документация и коммит.

ГЛАВНЫЙ БЛОКЕР: /v1/chat/completions зависает (100%+ локального CPU, не завершается),
из-за чего делегирование Hermes→Holix не работает. Всё остальное (образ 0.1.21,
gateway, auth hx_, /v1/models|capabilities) — готово.

═══════════════════════════════════════════════════════════
ИТЕРАЦИИ
═══════════════════════════════════════════════════════════

ИТЕРАЦИЯ 1 — Диагностика агентского CPU-loop (root cause)
  - Прочитать исходники Holix (core/agent_execution.py, LangGraph, обработку ответа
    LLM/reasoning_content, execution_mode, max_steps) — делегировано субагенту.
  - Включить debug-логи агента / посмотреть, где горит CPU (py-spy если есть).
  - Гипотезы: (a) reasoning-модель ломает ReAct-парсер; (b) LangGraph/react-цикл;
    (c) память/эмбеддинги; (d) first-run онбординг.
  Проверка: понятна причина зависания.

ИТЕРАЦИЯ 2 — Фикс модели агента (не-reasoning) — надёжно
  - Через Management API (admin hx_) выставить agent-models/провайдерский default_model
    = deepseek-chat (НЕ v4-flash) у coordinator; ИЛИ через holix config корректным ключом.
  - Убедиться, что агент реально использует deepseek-chat (не только config.model).
  - Тест /v1/chat/completions «PONG» с таймаутом.
  Проверка: чат отвечает быстро (<30с) ЛИБО причина не в модели → Итерация 3.

ИТЕРАЦИЯ 3 — Тюнинг агента (если модель не помогла)
  - По одному: execution_mode (react→?), max_steps↓ (напр. 6), отключить память/
    langgraph/subagents; тест после каждого.
  - Тест на ЧИСТОМ профиле (пустой /root/.holix) — исключить накопленную память.
  - Фоллбэк: попробовать /v1/runs (async) вместо chat; попробовать другую версию Holix
    (0.1.20/0.1.18) если loop версионный.
  Проверка: найдена рабочая конфигурация агентского исполнения.

ИТЕРАЦИЯ 4 — Раскатать рабочую конфигурацию на все 8 воркеров
  - Применить найденные настройки (модель/режим) ко всем holix-сервисам в compose.
  - Пересоздать партиями по 2-3, дождаться «Holix Agent ready», проверить health и
    один агентский прогон на роль (qa/loop-checker должны реально гонять тесты).
  Проверка: каждый воркер исполняет агентский запрос.

ИТЕРАЦИЯ 5 — Привести состав к замыслу
  - backend-executor → holix-python-dev: дать yaml-конфиг роли (developer, из
    backend-lead), workspace=бэкенд-код, том постоянный уже есть.
  - Создать holix-react-dev: контейнер + конфиг (из backend-lead, TS/JS), workspace=фронт.
  - Прописать делегирование: backend-lead→python-dev, frontend-lead→react-dev
    (в конфигах уже упомянуты эти имена).
  Проверка: 9 агентов (или сколько выйдет), делегирование в конфигах согласовано.

ИТЕРАЦИЯ 6 — E2E: делегирование Hermes→Holix
  - Проверить боевой путь: Hermes (хост) делегирует задачу coordinator через /v1
    (или holix task --delegate-to), coordinator отрабатывает. Минимум — воркер
    исполняет реальный агентский шаг через gateway с ключом.
  Проверка: сквозной прогон делегирования даёт результат.

ИТЕРАЦИЯ 7 — Чистка и документация
  - Дедуп навыка deploy-studio (я наплодил дубли Проблемы 17) — один чистый блок.
  - Чистка dangling-образов от неудачных сборок (docker image prune -f — аккуратно,
    только dangling).
  - Обновить: SESSION-HANDOFF.md (актуальное состояние), 01-architecture.md (реальный
    состав), PLAN-файлы (статусы).
  - Коммит в feature/holix-upgrade + push; при успехе — merge в main (по обстоятельствам).
  Проверка: git чист, remote SHA совпадает, навык без дублей.

ИТЕРАЦИЯ 8 — Финальная верификация + отчёт
  - Полный прогон: 8+ воркеров healthy, agentic-исполнение работает, auth, состав.
  - Утренний отчёт: что сделано, что осталось, честно про нерешённое.

СТАТУС: [ ]1 [ ]2 [ ]3 [ ]4 [ ]5 [ ]6 [ ]7 [ ]8
