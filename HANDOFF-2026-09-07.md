# HANDOFF — maxscrm деплой: main → прод (2026-09-07)

## СТАТУС: main → app.maxscrm.ru (прод) — РАБОТАЕТ ✅

app.maxscrm.ru → 200, health `database: ok, redis: ok`, витрина /s/demo → 200.
main — владелец прод-среды.

---

## ЧТО СДЕЛАНО

### Модель (как надо)
- **main → app.maxscrm.ru (прод)** — deploy-prod.yml, workflow_dispatch, version=main. ✅ успешно прогнан.
- **develop → стейджинг** — deploy-staging.yml отвязан от живого хоста (убран авто-триггер на пуш в develop; остался только ручной запуск для будущего отдельного стейджинг-сервера).

### Workflow-изменения (в develop и main)
- **deploy-prod.yml** переписан под РЕАЛЬНЫЙ прод-стек (app.maxscrm.ru): `~/maxscrm-staging`, порты backend 3001 / frontend 5175, nginx-проксирование, миграции, сид демо. Триггер: `workflow_dispatch` (push-триггер на main СЕЙЧАС ВЫКЛЮЧЕН — см. «Надо сделать»).
- **deploy-prod: безопасность** — при ошибке миграции НЕ сбрасывает БД (fail loudly, данные сохраняются). Убран опасный авто-sброс volume (он и убил БД в инциденте).
- **deploy-prod: БД-подключение** — миграция/сид байт-в-байт как у доказанно работающего deploy-staging: `postgresql://${STAGING_DB_USER:-maxscrm}:${ENC_PW}@localhost:5433/maxscrm` (миграция) и `postgresql://maxscrm:${ENC_PW}@localhost:5433/maxscrm` (сид). Пароль urlencode, юзер — напрямую (БЕЗ urlencode юзера — это была причина падений).
- **deploy-staging.yml** — убран авто-триггер develop, добавлен комментарий про отдельный стейджинг-сервер.

### Секреты (установлены в repo maxscrmru/maxscrm → Settings → Secrets → Actions)
- `PROD_HOST`, `PROD_USER`, `PROD_SSH_KEY`, `PROD_PORT` — SSH до прод-сервера (валидны).
- `PROD_DB_USER`, `PROD_DB_PASSWORD` — заданы, НО на этой БД НЕ работают (см. ниже).

### Релизы/деплой-раны
- Развилка main синхронизирована с develop (PR #1..#8 мержи, main = 1f1fb94).
- Deploy Production (main→прод) — SUCCESS (Build & Push Images + Deploy to Production).

---

## ВАЖНОЙ ФАКТ ПРО БД (чтобы не путаться)

Рабощее комбо подключения к БД на этом сервере — **`maxscrm` + `STAGING_DB_PASSWORD`** (доказано: им успешно гоняет deploy-staging и deploy-prod).
`PROD_DB_USER`/`PROD_DB_PASSWORD` (в т.ч. `imageshub`) на этой БД — **P1000 auth failed**.
Т.е. пользователь, который говорил «логин imageshub» — ошибается для данной БД; заходит `maxscrm`.
При разделении сред (прод на отдельном сервере) — там будут свои креды (PROD_DB_*), тогда и переключить.

---

## ИНЦИДЕНТЫ (кратко, для понимания рисков)
1. Первый запуск «Deploy Production» — упал `missing server host` (PROD_* секретов не было; workflow был заготовкой под несуществующий прод app.maxscrm.com).
2. Моя переработка deploy-prod — внёс баги в БД-подключение (urlencode username; захардкодил юзера в сиде) + деплой гонялся на общем со стейджингом стеке, а fallback сбрасывал volume → **сайт падал дважды**, БД дважды пересоздавалась (данные = демо/сид, восстановлено deploy-staging). Оба раза сайт вернули (recovery deploy-staging).
3. Финал: разобрал причину, починил БД-подключение, `main → прод` прошёл успешно.

---

## ЧТО НАДО СДЕЛАТЬ (TODO)

1. **[Рек.] Включить авто-деплой на мерж в main** (сейчас push-триггер выключен, деплой только вручную через workflow_dispatch). Сделать: в `.github/workflows/deploy-prod.yml` раскомментировать
   ```
   push:
     branches: [main]
   ```
   и смержить в main. ВНИМАНИЕ: этот мерж сам триггернет Deploy Production (~30 мин простоя, контент тот же).

2. **[Рек.] Починить красные E2E-тесты (Playwright) на develop** — НЕ регресс продукта, а pre-existing инфра-проблема тест-харнесса:
   - `playwright.config.ts` запускает backend как `npx nest start`, который падает на 93 pre-existing TS-ошибках (реально работает `ts-node --transpile-only`) → в E2E бэкенд не поднимается → сторефронт-тесты падают.
   - Демо-каталог (`demo-data.content.ts`) — инструменты (Дрель/Болгарка), а E2E-тест ждёт «Футболка UrbanLab Basic» из старого seed.ts → расхождение демо-данных.
   - Продукт собирается/деплоится корректно; это тестовый долг.

3. **[Опц.] Устранить расхождение демо-каталога** (content.ts vs seed.ts): решить, что должно быть в демо-витрине, и выровнять.

4. **[Опц.] Когда будешь разделять среды**: прод уедет на отдельный сервер → перенаправить deploy-prod на него (PROD_HOST/PROD_SSH_KEY/... + PROD_DB_*), а стейджинг — на новый dev-сервер (STAGING_HOST + STAGING_DB_*), deploy-staging вернуть в авто-режим на develop.

5. **[Опц.] Показать схему (archify)** прод-деплоя/приложения для наглядности — по желанию.

---

## ТЕКУЩЕЕ СОСТОЯНИЕ РЕПО
- Ветки: `main = 1f1fb94`, `develop = f1a885f` (локально develop чист; локальная ветка `order-tracking-rebase` сохранена — фича трекинга, PR #3).
- Git-рабочее дерево: чистое.
- Секреты: STAGING_* + NPM_TOKEN + PROD_* (6 шт) — установлены.
- Сайт: app.maxscrm.ru → 200 healthy.

## ПОЛЕЗНЫЕ КОМАНДЫ
- Запустить прод-деплой вручную: `workflow_dispatch` deploy-prod.yml, inputs.version=main.
- Восстановить сайт (если упал): workflow_dispatch deploy-staging.yml (ref=main).
- Ускорить/проверить БД-креды: см. tmp-db-check (удалён; при необходимости аналог).
- Раннеры self-hosted (hermes-host-runner / -2) — если зависли (busy=True, очередь не берётся): `systemctl --user restart actions-runner.service actions-runner-2.service`.
